vim.cmd.packadd 'nvim.undotree'

-- This plugin seems to move the cursor to the right window automatically, as
-- when using `nvim -d -c 'wincmd l'` without the `-c 'wincmd l'`
vim.cmd.packadd 'nvim.difftool'
